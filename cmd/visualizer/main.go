package main

import (
	"visualizer/pkg/logger"
)

func main() {
	logger.Info("Starting Audio Visualizer...")

	// 1. Initialize Audio Stream
	// audioStream := audio.NewAudioStream()
	// defer audioStream.Close()

	// 2. Start Capture
	// go audioStream.StartCapture()

	// 3. Start UI (Main Thread)
	// ui.Run()
}
